### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Address {
  city: String!
  id: ID!
  street: String!
  zip: String!
}

type Mutation {
  """Connect two people as friends. The relationship is bidirectional."""
  addFriend(friendId: ID!, personId: ID!): Person!
}

type Person {
  address: Address
  email: String!
  friends: [Person!]!
  id: ID!
  name: String!
}

type Query {
  """List all people — used to populate the Add Friend dropdown."""
  getPeople: [Person!]!

  """
  Fetch a person along with their address and friends. This is the single entry-point for the /person/[id] page.
  """
  getPerson(id: ID!): Person
}